[package]
name = "dedent-paste"
version = "0.1.1"
edition = "2024"
authors = ["Will 保哥"]
license = "MIT"
repository = "https://github.com/doggy8088/dedent-paste"

[dependencies]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
